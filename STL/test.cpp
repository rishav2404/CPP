#include <bits/stdc++.h>
using namespace std;

int main(){
    // set<int> st;
    // cout<<"enter the size of set:";
    // int q;
    // cin>>q;
    // for(int i=0;i<q;i++){
    //     int x;
    //     cin>>x;
    //     st.insert(x);
    // }

    // int k;
    // cout << "Enter the number to find : ";
    // cin >> k;
    // auto it = st.find(k);
    // if(it == st.end()) cout<<"Not found\n";
    // else cout<<"Found at"<<" ";

    // cout<<"Number to find Upper Bound : ";
    // int ubnum;
    // cin >> ubnum ;
    // auto it1 = st.upper_bound(ubnum);
    // cout<< *it1 << endl;

    // cout<<"Number to find  lower Bound : ";
    // int lbnum;
    // cin >> lbnum ;
    // auto it2 = st.lower_bound(lbnum);
    // cout<< *it2 << endl;

    /*------------------------------------------------*/


    // priority_queue<int> pq;
    // cout<<"enter the size of priority queue :";
    // int q;
    // cin>>q;
    // for(int i=0;i<q;i++){
    //     int x;
    //     cin>>x;
    //     pq.push(x);
    // }

    // pq.pop();
    
    
   /*------------------------------------------------*/

    // int a[] = {1,4,5,8,9};
    // bool result = binary_search(a,a+5,3);
    // bool result1 = binary_search(a,a+5,4);
    // cout << result << result1 << endl;
    // int ind1 = lower_bound(a,a+5,4)-a;
    // int ind2 = lower_bound(a,a+5,7)-a;
    // int ind3 = lower_bound(a,a+5,10)-a;

    // cout<<ind1<<" "<<ind2<<" "<<ind3<< endl;


    cout<< 2/10<<endl;
}